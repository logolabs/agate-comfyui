# Makes tests/ the rootdir: the repository root is a ComfyUI package (an __init__.py with
# relative imports) that pytest must not import as a plain module. test_nodes.py loads it
# as a package, the way ComfyUI does. Run as `python -m pytest tests` from the repo root.
[pytest]
addopts = -p no:cacheprovider
